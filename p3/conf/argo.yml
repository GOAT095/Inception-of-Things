apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-app
  namespace: argocd
spec:
  project: default
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: dev
  source: 
    repoURL: 'https://github.com/tchtaibii/tchtaibi-ception.git'
    targetRevision: master
    path: p3/conf/
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true